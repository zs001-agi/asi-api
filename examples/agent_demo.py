# Auto-generated by Wutong ASI agent
import requests

# Define the API endpoint URL
url = "https://example.com/api/v1/instance"

# Set headers to include your credentials or any required authentication information
headers = {
    "Authorization": "Bearer YOUR_API_KEY",
    "Content-Type": "application/json"
}

# Prepare the request body with instance details
data = {
    "model_name": "wutong_asi_5_local_models",
    "input": {
        # Replace with your input data for the model
        "text": "Hello, World!"
    }
}

# Send a POST request to the API endpoint
response = requests.post(url, headers=headers, json=data)

# Check if the request was successful
if response.status_code == 200:
    print("Instance created successfully!")
else:
    print(f"Failed to create instance. Status code: {response.status_code}")