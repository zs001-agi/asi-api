# Auto-generated by Wutong ASI agent
# Import necessary libraries
import requests
import json

# Define the URL for accessing the Wutong ASI API
url = "https://wutongasi.com/api"

# Define the headers and payload for the request
headers = {
    "Content-Type": "application/json"
}
payload = {
    "model_name": "self-evolving",
    "local_model_1": "model1.json",
    "local_model_2": "model2.json",
    "local_model_3": "model3.json",
    "local_model_4": "model4.json",
    "local_model_5": "model5.json"
}

# Send a POST request to the API
response = requests.post(url, headers=headers, json=payload)

# Check if the request was successful
if response.status_code == 200:
    # Parse the JSON response
    data = json.loads(response.text)
    
    # Print the response status and message
    print(f"Response Status: {data['status']}")
    print(f"Message: {data['message']}")
else:
    # Print an error message if the request failed
    print(f"Error: {response.status_code} - {response.text}")