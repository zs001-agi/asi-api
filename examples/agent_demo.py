# Auto-generated by Wutong ASI agent
import requests
import json

def main():
    # Define the API endpoint
    url = "https://wutong.asi.alibabacloud.com/v1/models"

    # Set the request headers
    headers = {
        "Content-Type": "application/json",
        "Authorization": "Bearer YOUR_ACCESS_TOKEN"
    }

    # Define the payload
    payload = {
        "model_name": "5_local_models",
        "description": "A self-evolving AI with 5 local models.",
        "version": "1.0",
        "author": "Wutong ASI Team"
    }

    # Send a POST request to create the model
    response = requests.post(url, headers=headers, json=payload)

    # Check the response status code
    if response.status_code == 201:
        print("Model created successfully:", response.json())
    else:
        print("Failed to create model:", response.status_code, response.text)

if __name__ == "__main__":
    main()