# Auto-generated by Wutong ASI agent
import requests

# Define the API endpoint
url = 'https://wutong-asi-api-docs.com/docs'

# Define the parameters
params = {
    'api_name': 'self-evolving_ai',
    'model_count': 5,
    'local_model_names': ['model1', 'model2', 'model3', 'model4', 'model5']
}

# Make a GET request to the API
response = requests.get(url, params=params)

# Check if the request was successful
if response.status_code == 200:
    # Parse the JSON response
    data = response.json()
    
    # Print the response
    print(data)
else:
    # Print an error message
    print(f'Failed to retrieve API documentation. Status code: {response.status_code}')