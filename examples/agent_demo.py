# Auto-generated by Wutong ASI agent
import requests

# Define the base URL of the Wutong ASI API
base_url = "https://api.example.com/v1"

# Example request to retrieve information about the AI system
response = requests.get(f"{base_url}/system/info")

# Check if the request was successful
if response.status_code == 200:
    # Parse the JSON response
    data = response.json()
    
    # Print the retrieved information
    print("AI System Information:")
    print(data)
else:
    # Print an error message if the request failed
    print(f"Failed to retrieve system info. Status code: {response.status_code}")