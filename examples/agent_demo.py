# Auto-generated by Wutong ASI agent
import requests

# Define the API endpoint
url = "http://localhost:8080/api"

# Send a GET request to the API
response = requests.get(url)

# Check if the request was successful
if response.status_code == 200:
    # Parse the JSON response
    data = response.json()
    
    # Print the version of Wutong ASI
    print(f"Wutong ASI Version: {data['version']}")
    
    # Print the number of local models
    print(f"Number of Local Models: {len(data['models'])}")
    
    # Print the list of local model names
    for model in data['models']:
        print(model)
else:
    # Print an error message if the request failed
    print(f"Failed to retrieve data. Status code: {response.status_code}")