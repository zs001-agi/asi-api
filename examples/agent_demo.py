# Auto-generated by Wutong ASI agent
# Import necessary libraries
import requests

# Define the API endpoint and parameters
url = "https://api.wutongai.com/v1/models"
params = {
    "local_models": ["model1", "model2", "model3", "model4", "model5"],
    "evolution": True,
    "update_interval": 60,
    "batch_size": 8
}

# Make a POST request to the API
response = requests.post(url, json=params)

# Check if the request was successful
if response.status_code == 200:
    print("Model settings updated successfully:", response.json())
else:
    print("Failed to update model settings. Status code:", response.status_code)