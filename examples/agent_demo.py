# Auto-generated by Wutong ASI agent
import requests
import json

# Define the API endpoint URL
url = "https://api.wutong.ai/v1/asi"

# Define the payload for self-evolving AI
payload = {
    "model": "self-evolving-ai",
    "local_model": "5"
}

# Set headers to accept JSON response
headers = {"Content-Type": "application/json"}

# Send a POST request to the API endpoint
response = requests.post(url, data=json.dumps(payload), headers=headers)

# Check if the request was successful
if response.status_code == 200:
    # Print the response content
    print(response.json())
else:
    # Print the error message
    print(f"Failed to retrieve self-evolving AI model. Status code: {response.status_code}")