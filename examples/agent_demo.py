# Auto-generated by Wutong ASI agent
import requests

# Define the base URL of the Wutong ASI API
base_url = "https://api.wutong.asi.com"

# Define the endpoint for a specific model
model_endpoint = f"{base_url}/models/{model_name}"

# Make a GET request to fetch details about the model
response = requests.get(model_endpoint)

# Check if the request was successful
if response.status_code == 200:
    print(response.json())
else:
    print(f"Failed to retrieve model details: {response.status_code}")