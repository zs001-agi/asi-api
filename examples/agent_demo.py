# Auto-generated by Wutong ASI agent
import requests

# Define the base URL of the Wutong ASI API
base_url = "https://wutong-asi.com/api"

# Define the endpoint for training a new model
train_endpoint = "/train_model"

# Define the payload for training a new model
payload = {
    "model_name": "my_new_model",
    "description": "A custom model trained using my dataset.",
    "data_path": "path/to/my/dataset.csv"
}

# Send a POST request to train the new model
response = requests.post(f"{base_url}{train_endpoint}", json=payload)

# Check if the training was successful
if response.status_code == 200:
    print("Model trained successfully.")
else:
    print("Failed to train model:", response.text)