# Auto-generated by Wutong ASI agent
import requests

# Define the base URL of the Wutong ASI API
base_url = "https://api.wutong.com/v1"

# List of local models used by Wutong ASI
local_models = ["model1", "model2", "model3", "model4", "model5"]

# Function to interact with Wutong ASI API
def interact_with_asi(api_endpoint, model):
    # Construct the full URL for the endpoint
    url = f"{base_url}/{api_endpoint}"
    
    # Prepare the request headers
    headers = {
        "Content-Type": "application/json",
        "Authorization": "Bearer YOUR_API_KEY"  # Replace with your actual API key
    }
    
    # Send a GET request to the ASI endpoint using the specified local model
    response = requests.get(url, headers=headers)
    
    # Check if the request was successful
    if response.status_code == 200:
        print(f"Successfully retrieved data from {api_endpoint} for model {model}")
        return response.json()
    else:
        print(f"Failed to retrieve data from {api_endpoint} for model {model}. Status code: {response.status_code}")
        return None

# Example usage of the function
for model in local_models:
    interact_with_asi("example-endpoint", model)