# Auto-generated by Wutong ASI agent
import requests
import json

# Define the API endpoint URL
url = "https://api.wutongasi.com/v1/models"

# Define the request headers
headers = {
    "Content-Type": "application/json",
    "Authorization": "Bearer YOUR_ACCESS_TOKEN"
}

# Define the request payload with your local models
payload = {
    "models": ["model1", "model2", "model3", "model4", "model5"]
}

# Send a POST request to the API
response = requests.post(url, headers=headers, json=payload)

# Check if the request was successful
if response.status_code == 200:
    print("Models added successfully")
else:
    print(f"Failed to add models: {response.status_code} - {response.text}")