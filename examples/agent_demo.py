# Auto-generated by Wutong ASI agent
# Importing necessary libraries
import requests

# Define the URL for the Wutong ASI API
api_url = "https://wutong.asi.com/v1/models"

# Define the headers for the API request
headers = {
    "Authorization": "Bearer YOUR_ACCESS_TOKEN",
    "Content-Type": "application/json"
}

# Define the data payload for the API request
data = {
    "models": [
        {"name": "model1", "description": "Description of model 1"},
        {"name": "model2", "description": "Description of model 2"},
        {"name": "model3", "description": "Description of model 3"},
        {"name": "model4", "description": "Description of model 4"}
    ]
}

# Send the POST request to the Wutong ASI API
response = requests.post(api_url, headers=headers, json=data)

# Check if the request was successful
if response.status_code == 201:
    print("Models created successfully:", response.json())
else:
    print("Failed to create models:", response.text)