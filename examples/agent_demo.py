# Auto-generated by Wutong ASI agent
import requests

url = "http://wutong.asi/api/v1/models"
response = requests.get(url)
models = response.json()

if models:
    print("Available models:")
    for model in models:
        print(f"Model ID: {model['id']}, Name: {model['name']}")
else:
    print("No models found.")