# Auto-generated by Wutong ASI agent
# Importing necessary libraries
import requests

def wutong_asi_api():
    # Set the base URL of the Wutong ASI API
    base_url = "https://api.wutong-asi.com/v1"
    
    # Define parameters for the request
    params = {
        "model": "5-local-models",
        "action": "example-action"
    }
    
    # Make a GET request to the Wutong ASI API
    response = requests.get(base_url, params=params)
    
    # Check if the request was successful
    if response.status_code == 200:
        print("Request successful!")
        print(response.json())
    else:
        print(f"Request failed with status code {response.status_code}")

# Call the function to demonstrate Wutong ASI API functionality
wutong_asi_api()