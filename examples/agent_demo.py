# Auto-generated by Wutong ASI agent
# Import necessary libraries
import requests

# Define the API endpoint URL
url = "https://wutong.asi.alibabacloud.com/v1/models"

# Prepare the request headers
headers = {
    "Content-Type": "application/json",
    "Authorization": "Bearer YOUR_ACCESS_TOKEN"
}

# Prepare the request body
payload = {
    "models": [
        {"name": "model1", "type": "text"},
        {"name": "model2", "type": "image"},
        {"name": "model3", "type": "audio"},
        {"name": "model4", "type": "video"},
        {"name": "model5", "type": "language"}
    ]
}

# Send the POST request
response = requests.post(url, headers=headers, json=payload)

# Check the response status code
if response.status_code == 201:
    print("Model creation successful")
else:
    print(f"Failed to create model: {response.status_code} - {response.text}")