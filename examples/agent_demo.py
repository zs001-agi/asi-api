# Auto-generated by Wutong ASI agent
import requests

# Define the base URL for the Wutong ASI API
base_url = "https://api.wutongasi.com/v1/"

# Define the headers for authentication (if required)
headers = {
    "Authorization": "Bearer YOUR_API_KEY"
}

# Example API call: Get information about a specific model
model_id = "example-model"
response = requests.get(f"{base_url}models/{model_id}", headers=headers)

# Check if the request was successful
if response.status_code == 200:
    model_info = response.json()
    print(model_info)
else:
    print(f"Failed to get model info: {response.status_code}")