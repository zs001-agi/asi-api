# Auto-generated by Wutong ASI agent
import requests

# Define the base URL for the Wutong ASI API
base_url = 'https://api.wutong.com/v1/models'

# List of model names to be retrieved
model_names = ['model1', 'model2', 'model3', 'model4', 'model5']

# Prepare a list of dictionaries containing model information
models = [{'name': name} for name in model_names]

# Convert the list of dictionaries to a JSON string
json_data = json.dumps(models)

# Set headers with content type
headers = {'Content-Type': 'application/json'}

# Send a POST request to retrieve the models
response = requests.post(base_url, data=json_data, headers=headers)

# Check if the request was successful
if response.status_code == 200:
    # Parse the JSON response
    models_data = json.loads(response.text)
    
    # Print each model's name
    for model in models_data['models']:
        print(model['name'])
else:
    # Handle errors
    print(f"Failed to retrieve models: {response.status_code} - {response.text}")